name: Ktlint

on: [pull_request]

jobs:
  Ktlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: "Install ktlint"
        uses: nbadal/action-ktlint-setup@v1
        with:
          ktlint_version: '1.6.0'
      - run: ktlint
        shell: bash
